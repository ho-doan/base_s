name: mocks
description: "Mock server template"
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

executables:
  core_gen: core_clean_gen

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  hodoan_lint: ^0.0.4
  lints: ^3.0.0
  test: ^1.25.2
